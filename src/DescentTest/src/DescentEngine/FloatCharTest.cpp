#include <gtest/gtest.h>

#include <DescentEngine/src/DataTypes/FloatChar.h>

TEST(FloatCharTest, set_get) {

	FloatCharZeroOne cc;
/*
	cc.set(0.0f);
	ASSERT_FLOAT_EQ(0.0f, cc.get());

	cc.set(1.0f);
	ASSERT_FLOAT_EQ(1.0f, cc.get());

	cc.set(0.4f);
	ASSERT_FLOAT_EQ(0.4f, cc.get());*/

	/*
	 ASSERT_TRUE(tsa.canActivate());
	 tsa.activate(1.0f);
	 ASSERT_FALSE(tsa.canActivate());
	 tsa.validate(0.5f, 0.0f);
	 ASSERT_FALSE(tsa.wasJustDisabled());
	 ASSERT_FALSE(tsa.canActivate());
	 ASSERT_TRUE(tsa.isActive());
	 tsa.validate(0.6f, 0.0f);
	 ASSERT_TRUE(tsa.wasJustDisabled());
	 ASSERT_FALSE(tsa.wasJustDisabled());
	 ASSERT_TRUE(tsa.canActivate());
	 ASSERT_FALSE(tsa.isActive());*/
}

